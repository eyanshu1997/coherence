Set up coherence on a teammate's machine (remote CLI mode — no local server).

Read ${COHERENCE_HOME:-~/coherence}/agents/setup-teammate-agent.md and follow the procedure defined there.

Arguments: $ARGUMENTS
