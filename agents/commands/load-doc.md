Load a doc folder (or URL) into context, including any browser comments left on those docs.

Read ${COHERENCE_HOME:-~/coherence}/agents/load-doc-agent.md and follow the procedure defined there.

Arguments: $ARGUMENTS
