Set up the coherence environment on a new VM: venv, .env, systemd service, nginx config, cron entries, and slash commands.

Read ${COHERENCE_HOME:-~/coherence}/agents/setup-agent.md and follow the procedure defined there.

Arguments: $ARGUMENTS
