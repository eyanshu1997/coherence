Generate a styled HTML summary document in ~/.coherence/data/. Ask for folder name and title if not provided, write content, run the coherence-doc CLI, report the URL. Do NOT check nginx, the comment server, or any other infrastructure.

Read ${COHERENCE_HOME:-~/coherence}/agents/generate-doc-agent.md and follow the procedure defined there.

Arguments: $ARGUMENTS
