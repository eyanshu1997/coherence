Load user comments from Claude Doc pages into context as feedback for the current session.

Read ${COHERENCE_HOME:-~/coherence}/agents/read-doc-comments-agent.md and follow the procedure defined there.

Arguments: $ARGUMENTS
