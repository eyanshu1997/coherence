Extract reusable skills and surface improvements to existing docs/commands/skills from the current session. Asks user to confirm before writing anything.

Read ${COHERENCE_HOME:-~/coherence}/agents/learn-agent.md and follow the procedure defined there.

Arguments: $ARGUMENTS
