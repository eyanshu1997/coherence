# Claude Code Preferences

dont ask confirmation for any commands specified in agent files below

**NEVER commit changes with git commit unless the user explicitly asks to commit. This applies even when a fix is complete, the build passes, and everything works. Always wait for explicit instruction before committing.**

**When creating git commits, NEVER add a Co-Authored-By line. Do not include any AI authorship attribution in commit messages.**

@COHERENCE_HOME/agents/generate-doc-agent.md
